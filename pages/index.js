import React from 'react'

import {Button} from "antd"

import "./App.less"

const Home = () => (
  <div>
    <Button type="primary" style={{ marginLeft: 8 }}>
      Primary Button
    </Button>
  </div>
)

export default Home
